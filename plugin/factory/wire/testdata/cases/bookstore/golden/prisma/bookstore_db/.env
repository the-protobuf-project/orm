# Connection URL for the bookstore_db database, pre-populated from the proto
# datasource url (regenerated with the schema; git-ignored). Edit locally if
# your credentials differ. bookstore_db.config.ts reads it via env("BOOKSTORE_DB_DATABASE_URL").
BOOKSTORE_DB_DATABASE_URL="postgresql://user:pass@localhost:5432/bookstore_db"
