# Connection URL for the wkt_db database.
# A ready-to-use .env is generated alongside this file; edit that one if your
# credentials differ. wkt_db.config.ts reads it via env("WKT_DB_DATABASE_URL").
WKT_DB_DATABASE_URL="postgresql://user:password@localhost:5432/wkt_db"
