# Connection URL for the users database.
# Copy this file to .env and replace the value with your real credentials.
# users.config.ts reads it via env("USERS_DATABASE_URL").
USERS_DATABASE_URL="postgresql://user:password@localhost:5432/users"
