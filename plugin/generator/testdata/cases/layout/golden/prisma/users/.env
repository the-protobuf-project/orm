# Connection URL for the users database, pre-populated from the proto
# datasource url (regenerated with the schema; git-ignored). Edit locally if
# your credentials differ. users.config.ts reads it via env("USERS_DATABASE_URL").
USERS_DATABASE_URL="postgresql://user:password@localhost:5432/users"
