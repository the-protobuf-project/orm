# Connection URL for the fleet database, pre-populated from the proto
# datasource url (regenerated with the schema; git-ignored). Edit locally if
# your credentials differ. fleet.config.ts reads it via env("FLEET_DATABASE_URL").
FLEET_DATABASE_URL="postgresql://user:password@localhost:5432/fleet"
