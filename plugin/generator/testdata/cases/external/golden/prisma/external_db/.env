# Connection URL for the external_db database, pre-populated from the proto
# datasource url (regenerated with the schema; git-ignored). Edit locally if
# your credentials differ. external_db.config.ts reads it via env("EXTERNAL_DB_DATABASE_URL").
EXTERNAL_DB_DATABASE_URL="postgresql://user:password@localhost:5432/external_db"
