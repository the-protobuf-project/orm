# Connection URL for the billing_db database.
# A ready-to-use .env is generated alongside this file; edit that one if your
# credentials differ. billing_db.config.ts reads it via env("BILLING_DB_DATABASE_URL").
BILLING_DB_DATABASE_URL="postgresql://user:password@localhost:5432/billing_db"
