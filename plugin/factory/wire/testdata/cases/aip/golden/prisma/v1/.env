# Connection URL for the v1 database, pre-populated from the proto
# datasource url (regenerated with the schema; git-ignored). Edit locally if
# your credentials differ. v1.config.ts reads it via env("V1_DATABASE_URL").
V1_DATABASE_URL="postgresql://user:password@localhost:5432/v1"
