# Connection URL for the events_db database, pre-populated from the proto
# datasource url (regenerated with the schema; git-ignored). Edit locally if
# your credentials differ. events_db.config.ts reads it via env("EVENTS_DB_DATABASE_URL").
EVENTS_DB_DATABASE_URL="mongodb://localhost:27017/events_db"
