# Connection URL for the billing_db database, pre-populated from the proto
# datasource url (regenerated with the schema; git-ignored). Edit locally if
# your credentials differ. billing_db.config.ts reads it via env("BILLING_DB_DATABASE_URL").
BILLING_DB_DATABASE_URL="postgresql://user:password@localhost:5432/billing_db"
