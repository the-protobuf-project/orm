# Connection URL for the external_db database.
# A ready-to-use .env is generated alongside this file; edit that one if your
# credentials differ. external_db.config.ts reads it via env("EXTERNAL_DB_DATABASE_URL").
EXTERNAL_DB_DATABASE_URL="postgresql://user:password@localhost:5432/external_db"
