# Connection URL for the v1 database.
# A ready-to-use .env is generated alongside this file; edit that one if your
# credentials differ. v1.config.ts reads it via env("V1_DATABASE_URL").
V1_DATABASE_URL="postgresql://user:password@localhost:5432/v1"
