# Connection URL for the reserved_db database, pre-populated from the proto
# datasource url (regenerated with the schema; git-ignored). Edit locally if
# your credentials differ. reserved_db.config.ts reads it via env("RESERVED_DB_DATABASE_URL").
RESERVED_DB_DATABASE_URL="postgresql://user:password@localhost:5432/reserved_db"
