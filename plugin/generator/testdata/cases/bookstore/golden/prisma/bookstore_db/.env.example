# Connection URL for the bookstore_db database.
# A ready-to-use .env is generated alongside this file; edit that one if your
# credentials differ. bookstore_db.config.ts reads it via env("BOOKSTORE_DB_DATABASE_URL").
BOOKSTORE_DB_DATABASE_URL="postgresql://user:pass@localhost:5432/bookstore_db"
