# Connection URL for the commerce database, pre-populated from the proto
# datasource url (regenerated with the schema; git-ignored). Edit locally if
# your credentials differ. commerce.config.ts reads it via env("COMMERCE_DATABASE_URL").
COMMERCE_DATABASE_URL="postgresql://user:password@localhost:5432/commerce"
