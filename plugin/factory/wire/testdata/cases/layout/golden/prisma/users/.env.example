# Connection URL for the users database.
# A ready-to-use .env is generated alongside this file; edit that one if your
# credentials differ. users.config.ts reads it via env("USERS_DATABASE_URL").
USERS_DATABASE_URL="postgresql://user:password@localhost:5432/users"
