# Connection URL for the wkt_db database, pre-populated from the proto
# datasource url (regenerated with the schema; git-ignored). Edit locally if
# your credentials differ. wkt_db.config.ts reads it via env("WKT_DB_DATABASE_URL").
WKT_DB_DATABASE_URL="postgresql://user:password@localhost:5432/wkt_db"
