# Connection URL for the commerce database.
# A ready-to-use .env is generated alongside this file; edit that one if your
# credentials differ. commerce.config.ts reads it via env("COMMERCE_DATABASE_URL").
COMMERCE_DATABASE_URL="postgresql://user:password@localhost:5432/commerce"
