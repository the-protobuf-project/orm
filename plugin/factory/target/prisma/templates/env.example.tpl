# Connection URL for the {{.Database}} database.
# A ready-to-use .env is generated alongside this file; edit that one if your
# credentials differ. {{.Database}}.config.ts reads it via env("{{.EnvVar}}").
{{.EnvVar}}="{{.URLExample}}"
