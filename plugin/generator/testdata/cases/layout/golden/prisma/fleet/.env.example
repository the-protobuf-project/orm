# Connection URL for the fleet database.
# Copy this file to .env and replace the value with your real credentials.
# fleet.config.ts reads it via env("FLEET_DATABASE_URL").
FLEET_DATABASE_URL="postgresql://user:password@localhost:5432/fleet"
