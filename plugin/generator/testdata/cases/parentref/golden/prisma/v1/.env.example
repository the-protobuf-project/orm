# Connection URL for the v1 database.
# Copy this file to .env and replace the value with your real credentials.
# v1.config.ts reads it via env("V1_DATABASE_URL").
V1_DATABASE_URL="postgresql://user:password@localhost:5432/v1"
