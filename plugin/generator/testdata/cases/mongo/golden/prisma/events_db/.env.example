# Connection URL for the events_db database.
# A ready-to-use .env is generated alongside this file; edit that one if your
# credentials differ. events_db.config.ts reads it via env("EVENTS_DB_DATABASE_URL").
EVENTS_DB_DATABASE_URL="mongodb://localhost:27017/events_db"
