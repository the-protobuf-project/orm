# Connection URL for the fleet database.
# A ready-to-use .env is generated alongside this file; edit that one if your
# credentials differ. fleet.config.ts reads it via env("FLEET_DATABASE_URL").
FLEET_DATABASE_URL="postgresql://user:password@localhost:5432/fleet"
