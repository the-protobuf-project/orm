# Connection URL for the reserved_db database.
# A ready-to-use .env is generated alongside this file; edit that one if your
# credentials differ. reserved_db.config.ts reads it via env("RESERVED_DB_DATABASE_URL").
RESERVED_DB_DATABASE_URL="postgresql://user:password@localhost:5432/reserved_db"
